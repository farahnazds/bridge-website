import type { Metadata } from "next";
import ComingSoon from "@/components/ComingSoon";

export const metadata: Metadata = { title: "Staff \& Permissions — Admin — Bridgetx" };

export default function AdminStaffPermissionsPage() {
  return <ComingSoon title="Staff \& Permissions" description="Staff and the permission matrix for your assigned clubs." />;
}
