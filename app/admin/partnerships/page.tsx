import type { Metadata } from "next";
import ComingSoon from "@/components/ComingSoon";

export const metadata: Metadata = { title: "Partnerships Consultants — Admin — Bridgetx" };

export default function AdminPartnershipsPage() {
  return <ComingSoon title="Partnerships Consultants" description="Referral pipeline and consultant assignments." />;
}
