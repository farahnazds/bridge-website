import type { Metadata } from "next";
import ComingSoon from "@/components/ComingSoon";

export const metadata: Metadata = { title: "Brand Partners — Admin — Bridgetx" };

export default function AdminBrandPartnersPage() {
  return <ComingSoon title="Brand Partners" description="Brand partner accounts and aggregate access." />;
}
